# Furrify-but-real
zcity furrify text feature lol
to make it work: create a new folder and place the file there, then open powershell and type this // python -m pip install keyboard pyperclip // 
then when its done hold shift and press mb2
open powershell and type // python furry_tool.py // or just right click and open as python

